package gostack_tutorials

func gostack_tutorials_bootstrap_pop() {

	// > `stack.Extract(RETURN_Card, POSITION_First)`
	// >> *removes and returns the first card in the stack*

}

func gostack_tutorials_bootstrap_push() {

	// > `stack.Add(insert)`
	// >> *adds a card to the beginning of the stack (see default enum types)*

}

func gostack_tutorials_bootstrap_indexOf() {

	// > `stack.Get(RETURN_Idx, POSITION_Card, cardToMatch, MATCH_Object)`
	// >> *returns the index of the first found matching card*

}

func Bootstrap() {

	gostack_tutorials_bootstrap_pop()
	gostack_tutorials_bootstrap_push()
	gostack_tutorials_bootstrap_indexOf()

}
